# ansible-alpine-setup

Usage:

    git clone https://github.com/carlo-s/ansible-alpine-setup
    cd ansible-alpine-setup
    < Change the config.yml file! >
    ./start-alpine-setup
    sudo shutdown -h now
    
Issues/Todos:
...
